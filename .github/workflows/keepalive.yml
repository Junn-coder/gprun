name: Keepalive

on:
  schedule:
    - cron: '0 0 1 * *'  # 每月 1 号执行
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - name: Keep repository active
        run: echo "Keep the repository activity fresh for scheduled tasks."